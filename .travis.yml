os:
- linux
language: rust
rust:
- nightly
cache: 
- apt
- cargo

before_install:
  - sudo add-apt-repository ppa:jonathonf/python-3.6
  - sudo apt-get update
  - sudo apt-get install python3.6
addons:
  apt:
    update: true

script: make run