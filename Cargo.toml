[package]
name = "{{project-name}}"
version = "0.1.0"
authors = ["{{authors}}"]

[lib]
name = "{{project-name}}"
crate-type = ["cdylib"]

[dependencies.pyo3]
version = "*"
features = ["extension-module"]

[dependencies]
